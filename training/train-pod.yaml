apiVersion: v1
kind: Pod
metadata:
  name: mnist-train
  labels:
    app: mnist-train
spec:
  restartPolicy: Never
  containers:
  - name: mnist-train
    image: ghcr.io/bmppa/mnist:train
    imagePullPolicy: Always
    command: ['sh', '-c', 'python3 main.py --epoch 1 --save-model && sleep infinity']
    resources:
      requests:
        memory: "1000Mi"
      limits:
        memory: "1000Mi"
    volumeMounts:
    - name: model-storage
      mountPath: /app/model
  volumes:
  - name: model-storage
    hostPath:
      path: /tmp/mnist-models
      type: DirectoryOrCreate
  imagePullSecrets:
  - name: my-secret